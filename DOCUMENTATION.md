# مستندات پروژه هتل زاگرس (Hotel Zagrous)

این مستند شامل توضیحات کامل در مورد ویژگی‌ها و نحوه پیاده‌سازی پروژه هتل زاگرس است.

## ۱. ساختار کلی پروژه
پروژه از معماری **Multi-module** استفاده می‌کند:
- **`:app:shared`**: منطق مشترک (KMM)، شامل ویژگی‌های UI، ViewModelها و ریپازیتوری.
- **`:server`**: بخش سرور (Ktor) برای مدیریت داده‌ها.
- **`:core`**: مدل‌های داده‌ای مشترک و ابزارهای کاربردی (Utils).

## ۲. معماری نرم‌افزار
پروژه از الگوی **MVI (Model-View-Intent)** استفاده می‌کند:
- **State**: وضعیت فعلی صفحه را نگه می‌دارد.
- **Intent**: عملیات‌هایی که کاربر انجام می‌دهد.
- **ViewModel**: مدیریت منطق تجاری و ارتباط با Repository.

## ۳. ویژگی‌های بخش رزرو غذا (User Side)
این بخش در مسیر `app/shared/.../features/reservation` پیاده‌سازی شده است.

### ۳.۱. ورود به سیستم
- کاربران با **شماره اتاق** و **کد شناسایی** وارد می‌شوند.
- اعتبارسنجی در `ReservationViewModel` انجام می‌شود و در صورت تطابق، اطلاعات اتاق و رزروهای قبلی بارگذاری می‌گردد.

### ۳.۲. منطق انتخاب غذا بر اساس تاریخ
منو به صورت پویا بر اساس تاریخ شمسی تغییر می‌کند (`getFoodsForDate`):
- **روزهای زوج/فرد**: منو بر اساس زوج یا فرد بودن روز ماه تغییر می‌کند.
- **جمعه‌ها**: منوی مخصوص روز جمعه نمایش داده می‌شود.
- **شرط نمایش**: غذاهایی نمایش داده می‌شوند که تیک `isActive` و `isVisibleToUsers` آن‌ها در پنل ادمین فعال باشد.

### ۳.۳. ثبت رزرو
- کاربر می‌تواند برای ناهار و شام هر روزِ اقامت خود غذا انتخاب کند.
- رزروها به صورت دسته‌ای (Batch) در `HotelRepository` ذخیره می‌شوند.

## ۴. ویژگی‌های پنل مدیریت (Admin Panel)
این بخش در مسیر `app/shared/.../features/admin` پیاده‌سازی شده است.

### ۴.۱. مدیریت اتاق‌ها
- امکان افزودن اتاق جدید.
- امکان ویرایش اطلاعات اقامت (نام مهمان، کد ملی، تاریخ ورود و خروج، تعداد نفرات).

### ۴.۲. مدیریت غذاها و منو
- افزودن، ویرایش و حذف غذاها (`upsertFood`, `deleteFood`).
- تنظیم ترتیب نمایش (`displayOrder`).
- مدیریت تنظیمات منو (`MenuConfig`): فعال یا غیرفعال کردن کل منوی ناهار/شام برای روزهای خاص (زوج/فرد/جمعه).

### ۴.۳. گزارش‌گیری و چاپ
- تولید گزارش روزانه به صورت HTML.
- نمایش جدولی شامل شماره اتاق، تعداد غذا و تفکیک نوع غذاهای سفارش داده شده.
- استفاده از `ReportPrinter` برای چاپ مستقیم از اپلیکیشن.

### ۴.۴. مدیریت توزیع غذا
- ادمین می‌تواند ناهار یا شام هر مهمان را پس از تحویل، به عنوان "تحویل شده" (`MarkDelivered`) علامت‌گذاری کند.

## ۵. بخش داده و شبکه (Data Layer)
- **Ktor Client**: برای ارتباط با API سرور در `HotelRepository` استفاده شده است.
- **Serialization**: از `kotlinx-serialization` برای تبدیل داده‌ها استفاده می‌شود.
- **نرمال‌سازی**: تمامی اعداد (شماره اتاق و کد ملی) قبل از ارسال به سرور توسط `normalizeDigits` به اعداد انگلیسی تبدیل می‌شوند.

## ۶. تکنولوژی‌های مورد استفاده
- **Kotlin Multiplatform (KMP)**
- **Jetpack Compose** (برای رابط کاربری)
- **Ktor** (برای شبکه)
- **MVI Architecture**
- **HTML/CSS** (برای تولید گزارشات چاپی)
